# JSON Schema for API

The networked game has JSON API that is always the same, in a sense that it conforms to the schema in this folder. 

If you ever get an error and are able to download the JSON from the UI, you may be able to identify the issue by running:

