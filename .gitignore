/build
gomoku
gomoku-httpd
gomoku-http-client
test_gomoku
test_daemon
**/.DS_Store
**/*.log
tests/googletest
# CMake specific files and directories
/build*/                  # Common build directories (e.g., build/, build-release/, etc.)
/out/                     # Another common build directory name
CMakeCache.txt            # CMake cache file
CMakeFiles/               # Directory for generated build system files
cmake_install.cmake       # Installation script
install_manifest.txt      # Manifest of installed files
CTestTestfile.cmake       # CTest generated file
CmakeScripts/             # Directory for generated scripts
Testing/                  # Directory for test results
_deps/                    # Directory for dependencies managed by CMake's FetchContent
compile_commands.json     # Optional, but often generated and ignorable

# IDE-specific directories and files
/.idea/                   # CLion IDE files
.vscode/                  # VS Code settings (optional, can be kept if desired)
*.VC.opendb               # Visual Studio files
*.VC.db
/bld/                     # Visual Studio build directory
.vs/                      # Visual Studio cache/options directory
*.user                    # User-specific files for various IDEs (e.g., CMakeLists.txt.user)
**/*.o
**/*.a

# Compiled object files and executables
*.o                       # Object files
*.obj
*.d
*.os
*.exe                     # Executables (Windows)
*.out                     # Executables (Unix-like)
*.app                     # Executables (macOS)
*.dll                     # Dynamic libraries (Windows)
*.lib                     # Static/Import libraries (Windows)
*.so                      # Shared libraries (Unix-like)
*.a                       # Static libraries (Unix-like)
*.dylib                   # Dynamic libraries (macOS)

# Compiler/Linker output
*.map
*.stackdump
*.manifest

# Miscellaneous
.DS_Store                 # macOS Finder files
*.pyc                     # Python compile output (if using Python with CMake)
/*.json
/games/*.json
/tests/eval/results/tournament_*.txt

# Vendored library build artifacts
src/vendor/json-c/build/

